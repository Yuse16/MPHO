'use client'
import { useState } from 'react'
export function PaymentRequery({attemptId}:{attemptId:string}){const[reason,setReason]=useState('');const[status,setStatus]=useState<string|null>(null);async function run(){setStatus('Consultando…');const response=await fetch(`/api/payments/${attemptId}/requery`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({reason})});setStatus(response.ok?'Reconsulta completada. Actualiza la página.':'No fue posible reconsultar. Verifica el rol, la razón o el cooldown.')}return <section className="mt-6 rounded-2xl border border-white/10 p-5"><h2 className="font-bold">Acción auditada</h2><label className="mt-4 block text-sm">Razón<input value={reason} onChange={e=>setReason(e.target.value)} className="mt-2 w-full rounded-xl border border-white/15 bg-transparent p-3"/></label><button onClick={run} disabled={reason.trim().length<8} className="mt-4 rounded-full bg-[#c8ff35] px-5 py-3 font-bold text-black disabled:opacity-50">Reconsultar proveedor</button>{status&&<p aria-live="polite" className="mt-3 text-sm text-[#bbc3be]">{status}</p>}</section>}
