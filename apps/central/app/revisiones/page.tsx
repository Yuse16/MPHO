import Link from 'next/link'
import { listReviews } from '@/lib/reviews'
export const dynamic='force-dynamic'
export default async function ReviewsPage(){const result=await listReviews();return <main className="min-h-screen p-5 sm:p-10"><div className="mx-auto max-w-5xl"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#c8ff35]">MPHO Central</p><h1 className="mt-2 text-3xl font-bold">Revisiones operativas</h1>{!result.ok?<div role="alert" className="mt-8 rounded-2xl border border-white/10 p-5">Acceso no autorizado o servicio no disponible.</div>:result.value.length===0?<div className="mt-8 rounded-2xl border border-white/10 p-5">No hay revisiones pendientes.</div>:<div className="mt-8 grid gap-4">{result.value.map(review=><Link key={review.orderId} href={`/revisiones/${review.orderId}`} className="rounded-2xl border border-white/10 bg-white/[.03] p-5"><div className="flex flex-wrap justify-between gap-3"><div><p className="font-bold">{review.reference}</p><p className="mt-1 text-sm text-[#bbc3be]">{review.recipient.name} · {review.address.street}</p></div><div className="text-right text-sm"><p>{review.reviewStatus}</p><p className="text-[#bbc3be]">{review.nextAction}</p></div></div></Link>)}</div>}</div></main>}
